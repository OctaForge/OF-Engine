import core.logger as log

log.log(log.DEBUG, ":::: Frame handling.")
import core.events.frame as nil

log.log(log.DEBUG, ":::: Signal system.")
import core.events.signal as nil

log.log(log.DEBUG, ":::: Action system.")
import core.events.actions as nil

log.log(log.DEBUG, ":::: World events.")
import core.events.world as nil

log.log(log.DEBUG, ":::: Input events.")
import core.events.input as nil
